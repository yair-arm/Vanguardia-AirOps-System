# Vault interno de trabajo (documentación de conocimiento del proyecto)
Vault/
.obsidian/

# Compilación y binarios
build/
*.o
*.obj
*.exe
*.out
*.ilk
*.pdb
compile_commands.json

# Datos iniciales del sistema (versionados para que el repo sea autocontenido;
# el programa reescribe el archivo localmente en runtime)
datos_local.json
